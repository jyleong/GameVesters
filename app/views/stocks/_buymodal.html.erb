<div id="BuyModal" class="modal fade" role="dialog">
	<div class="modal dialog">
		<div class="modal-content">
			<div class="modal-header">
					<button type="button" class="btn btn-default" data-dismiss="modal">&times;
					</button>
					<h4 class="modal-title">Buy</h4>
				</div>
			<%= form_tag new_user_transaction_path(user: current_user, user_id: current_user.id, stock_symbol: @stock.symbol, stock_id: @stock.id ? @stock.id : '', stock_price: @stock.current_price), remote: true, method: "post", id: 'stock-buy-form' do %>
				<div class="form-group row no-padding text-center col-md-12">
					<div class="col-md-10">
					<p><%= "#{@stock.symbol} current price: #{@stock.current_price}"%></p>
				  	<%= number_field_tag :quantity, params[:quantity], placeholder: "Quantity", autofocus: true,
					class: 'form-control' %>
				  
					</div>
				<div class="col-md-2">
			  
			 	<%= button_tag(type: :submit, class: "btn btn-success") do %>
				Buy
				<% end %>
				</div>
				</div>
			<% end %>
		</div>
	</div>

</div>